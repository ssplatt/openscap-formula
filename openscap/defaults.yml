# -*- coding: utf-8 -*-
# vim: ft=yaml
# Defaults for openscap

openscap:
  enabled: false
  mockup: false
  policy_server: false
  pkg: libopenscap8
  policy_server_pkg: ssg-debian
  service:
    name: oscap
    enable: false
    state: dead
